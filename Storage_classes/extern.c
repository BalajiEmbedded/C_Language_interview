//Declares a global variable without creating it.
//Used to share a global variable across multiple files.
//Default value: 0 (if not initialized).
//Storage: Data segment / BSS segment (because it refers to global/static variables).
//Lifetime: Entire program execution.

